# Krkr2Packer 使用说明

Krkr2Packer 是一款 Windows 桌面工具：把 PC 端 **Kirikiri2（吉里吉里2）引擎**的游戏
（目录下有 `.xp3` 数据包）一键打包成**安卓直装 APK**——安装后点图标自动解压并直接进入
游戏，无需文件选择器。

软件以一份"瘦身壳模板 APK"为唯一模板，每次打包流程为：
**解包模板 → 注入新游戏 → 改包名/应用名/图标 → 回编译 → 签名 → 自动验收**。

---

## 一、环境准备

| 依赖 | 要求 | 检查方法 |
|---|---|---|
| Python | 3.10+ | `python --version` |
| Pillow | 任意较新版本 | `pip install -r requirements.txt` |
| JDK | 17+（java / keytool / jarsigner 在 PATH） | `java -version` |
| apktool.jar | 2.x | 首次使用自动下载，或手动放置 |
| zipalign / apksigner | 仅 targetSdk≥30 需要 | 可选，来自 Android build-tools |

1. **安装 Python**：到 https://www.python.org 安装 3.10+，勾选 *Add to PATH*。
2. **安装 JDK 17+**：推荐 Adoptium Temurin（https://adoptium.net ，选 Windows x64 的
   JDK 17 或 21，MSI 安装时勾选 *Set JAVA_HOME* 与 *Add to PATH*）。
   装好后开命令行执行 `java -version` 能出版本号即可（jarsigner/keytool 与 java 同目录，
   软件会自动找到）。
3. **安装 Pillow**：`pip install -r requirements.txt`（只需 Pillow 一个依赖）。
4. **apktool.jar**：软件按以下顺序查找——「高级设置」里指定的路径 → 项目 `tools/apktool.jar`
   → 自动从 GitHub 官方 Release 下载（约 22MB，需联网）。下载失败时请手动到
   https://github.com/iBotPeaches/Apktool/releases 下载 `apktool_x.x.jar` 放进 `tools/`。
5. **（可选）build-tools**：只有当你强行把 targetSdk 改到 ≥30 时才需要
   zipalign + apksigner。安装 Android Studio 或 Android 命令行工具后，
   在「高级设置」里指定 build-tools 目录即可；软件也会自动搜索
   `%LOCALAPPDATA%\Android\Sdk\build-tools` 与 `ANDROID_HOME`。

---

## 二、快速上手

> **启动方式**：双击项目目录里的 **`启动打包工具.bat`** 即可打开界面（自动优先使用
> pythonw，不弹黑色命令行窗口）；也可以命令行 `python main.py`。
> **输入框技巧**：所有输入框支持**右键菜单**（剪切/复制/粘贴/删除/全选）；包名、密码
> 两个输入框已禁用输入法，直接打字母/数字/点号（含小键盘）即可，无需切换输入法。

1. 双击运行 `启动打包工具.bat`（或 `python main.py`）。
2. **模板设置**：选择瘦身壳模板 APK（如"直装包模板壳.apk"）。首次使用会自动解包到
   `%LOCALAPPDATA%\Krkr2Packer\template\`，随后弹出内核指纹（libgame.so 的 SHA256）
   确认框——点"是"接受后写入配置，之后每次自动校验，不一致会警告模板可能被篡改。
3. **游戏设置**：选择游戏目录（含 `.xp3`，支持中文路径）。软件自动列出**全部** xp3
   （无论体积大小、有无加密魔数，避免漏掉小体积启动文件）与引擎检测结果，并按规则
   推断**启动文件**（`data.xp3` 优先 → 文件名含 data/init/start → 体积最大），下拉框
   可改选，也可以在列表里双击某个文件设为启动文件。
4. **应用信息**：填包名（如 `com.mycompany.mygame`，下方实时预览资源解压路径）与
   应用名（支持全中文），可选一张图标图（PNG/JPG/ICO，自动生成五档分辨率并圆角预览）。
   包名输入兼容中文输入法：全角句号"。"自动转"."、大写自动转小写、自动去空格，
   直接打字即可，无需切换输入法或复制粘贴。
   **游戏目录里的所有文件和子目录都会打包**（含 plugin/、savedata/、游戏自带字体等），
   仅与引擎运行时文件精确同名时跳过（recentpath.xml、ui、img、locale、font、Default、
   others、DroidSansFallback.ttf、default.cur）。
5. 点 **开始打包**。进度条按管线 13 步推进，子进程输出实时进日志区。完成后弹窗显示
   输出路径、体积与 SHA-256，并自动抽查验收（Manifest 包名 / recentpath.xml / 图标 /
   xp3 数量）。
6. 把 APK 传到安卓手机安装：安卓 14 会弹一次"**此应用是为旧版安卓打造的**"，点一下
   继续即可；之后打开图标 → 自动解压 → 直接进游戏，**第二次打开直达游戏**。

> 换包名打包无需卸载旧游戏（两个 App 并存）；**同包名**换游戏必须先卸载旧版，
> 或在高级设置里"安装标记改名"后覆盖安装（会强制重新解压，约等待 1~2 分钟）。

---

## 三、模板瘦身壳的制作方法（附录）

软件**不自己造壳**。模板 = 一份现成的 Kirikiroid2 直装包（如《千恋\*万花》直装包）
**删除全部游戏资源**后的产物，体积约 35~40MB。制作步骤：

1. `apktool d 原直装包.apk -o work`（需 Java 8+，apktool 2.x）。
2. 删除 `assets/` 下所有 `*.xp3`，以及**非白名单**的游戏专属目录/文件
   （plugin/、video/、bgm、savedata、patch 等）。
3. **白名单（必须原样保留，一个都不能删）**：

   | 保留项 | 作用 |
   |---|---|
   | `assets/recentpath.xml` | 游戏入口钥匙，每次打包由本软件重写 |
   | `assets/ui/`（含 RecentListItem.csb） | **魔改版引擎界面 CSB，核心机关，绝不可动** |
   | `assets/img/` `assets/locale/` `assets/font/` | 引擎运行时资源 |
   | `assets/Default/` `assets/others/`（若存在） | 引擎运行时资源 |
   | `assets/*.ttf/*.otf/*.cur`（如 DroidSansFallback.ttf、default.cur） | 引擎字体/光标 |
   | `lib/` 整个目录（libgame.so / libSDL2.so / libffmpeg.so） | 引擎内核 |
   | `smali/` 整个目录（MainActivity + Kirikiroid2 引擎入口） | 解压壳逻辑 |
   | `res/`、`AndroidManifest.xml`、`apktool.yml` | 图标/应用名/版本信息载体 |

4. `apktool b work -o 直装包模板壳.apk`，再签名（jarsigner 或 apksigner 均可）。
5. **记录内核指纹**：分别计算 `lib/arm64-v8a/libgame.so` 与
   `lib/armeabi-v7a/libgame.so` 的 SHA-256。首次在软件中加载模板时会弹出确认框，
   接受后自动保存（软内也内置自检，指纹不符会警告"模板可能被篡改"）。

**模板内核说明**：内核 `libgame.so` 与官方 Kirikiroid2 1.3.9 逐字节一致，而 1.3.9 就是
官方最终版本（GitHub zeas2/Kirikiroid2，2018 年后未再更新），**无需也不得更换内核**。

---

## 四、高级设置详解

- **minSdk / targetSdk**：默认锁定"跟随模板"（minSdk 19 / targetSdk 26，**推荐**）。
  - 不写 uses-sdk → 沿用模板原值：兼容性最好，安卓 14 只弹一次"为旧版安卓打造"。
  - targetSdk 23~29：启用运行时权限；模板壳不含 requestPermissions 逻辑，存储权限
    申请会失效，并出现老安卓兼容行为（如点击屏幕边缘拉伸全屏）。
  - targetSdk ≥ 30：安卓 11+ 强制 v2 签名（v1-only 直接拒装报"解析错误"），流程变为
    apktool b → `zipalign -f 4` → `apksigner sign`；且 Manifest 中所有带 intent-filter
    的组件必须显式 `android:exported="true"`（软件会自动补齐）。未装 build-tools 时
    软件会明确报错并给出解决路径。
- **versionCode / versionName**：留空跟随模板；填写则修改 apktool.yml 的 versionInfo 段。
- **签名**：默认首次自动生成 keystore（RSA 2048，有效期 10000 天，CN=Game，保存到
  `%LOCALAPPDATA%\Krkr2Packer\keystore\krkr2packer.jks`），之后复用；也可导入已有 jks。
  **自动生成 keystore 的默认口令为 `krkr123456`、别名为 `game`**（已预填在高级设置里，
  点"显示"可查看，可修改）。若提示"keystore 校验失败"，请核对密码与别名是否和生成时
  一致；文件确已损坏时可删除后重新打包（自动重新生成，但换 keystore 后同包名旧版必须
  先卸载再装）。
- **权限清理**：列出模板全部 uses-permission，勾选保留（默认全保留）。删除
  READ_PHONE_STATE 可能导致广告 SDK getDeviceId 崩溃；删除 WRITE_EXTERNAL_STORAGE
  可能导致引擎写入失败。
- **安装标记改名**：模板壳解压完成后会写一个标记文件（如 `youlagou_ready_v2`），
  存在则跳过解压直接进游戏。改成新的随机串后，同包名覆盖安装会强制重新解压——
  **同包名换游戏时必用**（否则新游戏不会被打进去）。
- **输出文件名模板**：默认 `{appname}直装包.apk`，`{appname}` 会被替换为应用名
  （非法文件名字符自动转为下划线）。

---

## 五、常见问题（FAQ）

**Q8：包名里打不出点号/打出来是"。"？**
中文输入法的中文标点模式会把 `.` 变成全角句号"。"。软件已自动把"。"/"．"转换成"."、
大写转小写、去空格，正常打字即可；若粘贴带大写或全角字符也会自动纠正。

**Q9：换游戏但游戏目录里有字体、视频等非 xp3 文件会打包吗？**
会。游戏目录里的**所有文件与子目录**都原样打进 assets（ apktool 原样打包），
只有与引擎运行时文件精确同名的（见上文白名单）才会跳过并写警告。

**Q1：安装时提示"解析错误/无法安装"？**
最常见原因是签名形态与 targetSdk 不匹配：targetSdk ≥ 30 的包必须 v2 签名。要么把
targetSdk 改回"跟随模板"(26)，要么安装 build-tools 并在高级设置指定目录。

**Q2：点图标后黑屏/进了文件选择器？**
`assets/ui/` 被官方原版 Kirikiroid2 资源覆盖过，丢了"全屏按钮"魔改机关。请换用
合格的瘦身壳模板（见第三节），**绝不可**用官方资源覆盖 ui/。

**Q3：提示"找不到游戏"？**
recentpath.xml 指向的路径必须与解压目标一致。检查是否改过包名后没有重新打包；
同包名覆盖安装了新游戏但没改安装标记——先卸载旧版，或改标记后重装。

**Q4：中文游戏名/中文路径会出问题吗？**
不会。子进程输出按 UTF-8 读取（非法字节替换显示），文件操作全部显式 UTF-8 编码。
工作目录放在 `%TEMP%\Krkr2Packer\` 下以尽量缩短路径，避免 apktool 对超长路径的偶发问题。

**Q5：成品 APK 超大怎么办？**
超过 2GB 会警告（部分机型/工具兼容性风险），超过 4GB 严重警告（zip64 与签名工具
兼容问题）。建议精简游戏资源（视频/BGM 压缩，或拆分素材包）。

**Q6：apktool 下载失败？**
检查网络；或手动下载 jar 放到项目 `tools/apktool.jar`，或在高级设置指定路径。

**Q7：打包中途关闭/取消会有残留吗？**
打包在后台线程进行，可随时点"取消"。成功后临时工作目录自动清理；失败时保留
`%TEMP%\Krkr2Packer\build_xxx` 供排查，可手动删除。

**Q8：包名里打不出点号/打出来是"。"？**
三种已知情况都已处理：① 中文输入法的中文标点模式会把 `.` 变成全角句号"。"——软件
自动把"。"/"．"转换成"."、大写转小写、去空格；② **小键盘（数字区）的点号**在
Windows Tk 里有个老 bug，会被当成 Delete 键（表现为删掉光标后一个字符）——软件已
全局拦截修复；③ **包名、密码两个输入框已直接禁用输入法**，任何输入法状态下都能
直接打字母、数字、点号（主键盘和小键盘都行），不用切英文模式。

**Q8b：用中文输入法在输入框里打的英文，要点一下输入框才显示出来？**
这是 Windows Tk 处理输入法（TSF）的老毛病——文本其实已提交，只是没重绘。软件已在
每次按键后强制重绘，多数情况已消失；个别输入法若仍复现，给两个建议：打英文时按
**Shift** 切到输入法英文模式（微软拼音标准操作），或改用右键菜单粘贴。

**Q9：游戏目录里有字体、视频等非 xp3 文件会打包吗？**
会。游戏目录里的**所有文件与子目录**都原样打进 assets，只有与引擎运行时文件
精确同名的（recentpath.xml / ui / img / locale / font / Default / others /
DroidSansFallback.ttf / default.cur）才会跳过并在日志里写警告。

---

## 六、工作原理（一览）

1. 打开 App → `MainActivity.onCreate` → checkPermission → startTransform。
2. `isInstall2()`：检查 `Android/data/<包名>/files/` 下有无安装标记文件，有则直接启动
   引擎 Activity（org.tvp.kirikiri2.KR2Activity）。
3. 无标记则开线程把 **assets 全量**拷到 `Android/data/<包名>/files/`，同时把
   `assets/recentpath.xml` 拷到内部存储 `.preference/recentpath.xml`，写标记，启动引擎。
4. 引擎按 recentpath.xml 里的路径读游戏 xp3；Java 层在 1100ms/1200ms 于坐标 (100,100)
   模拟点击一次。
5. **关键机关**：模板 `assets/ui/RecentListItem.csb` 被魔改——btn_play 按钮放大到
   1280×1080 全屏，因此 (100,100) 必命中"开始游戏"，文件选择器一闪而过，实现全自动
   直进游戏。

这就是为什么：recentpath.xml 必须重写为
`/storage/emulated/0/Android/data/<新包名>/files/assets/<启动xp3名>`；
ui/ 等运行时资源绝不可被官方版覆盖；libgame.so 绝不可更换。

---

## 七、项目文件

```
Krkr2Packer/
  main.py              GUI 入口（tkinter/ttk）
  requirements.txt     仅 Pillow
  core/
    config.py          路径常量、白名单、配置持久化
    utils.py           子进程实时日志、JDK/apktool 检测与下载、sha256
    template.py        模板解包/缓存/结构校验/内核自检
    game.py            游戏扫描、XP3 魔数检测、启动文件推断
    manifest_edit.py   包名/应用名/exported/apktool.yml/smali 替换
    icon_gen.py        Pillow 多分辨率图标生成与替换
    builder.py         打包管线编排（13 步）
    signer.py          keystore 生成、jarsigner v1、zipalign+apksigner v2
  tools/apktool.jar    首次使用自动下载（或手动放置）
  使用说明.md          本文件
```
